services:
  mosquitto:
    image: eclipse-mosquitto:2.0
    container_name: visipick-mqtt
    ports:
      - "1883:1883"     # MQTT
      - "9001:9001"     # WebSocket (Blazor용)
    volumes:
      - ../mosquitto/config:/mosquitto/config
      - ../mosquitto/data:/mosquitto/data
      - ../mosquitto/logs:/mosquitto/log
    restart: unless-stopped
